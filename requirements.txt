# =============================================================================
# 安装: pip install -r requirements.txt
# 本地开发（含音频模型）: pip install -r requirements.txt && pip install torch transformers diffusers accelerate soundfile safetensors
# =============================================================================

# --- Web 框架 ---
fastapi>=0.116,<1.0
uvicorn[standard]>=0.35,<1.0
httpx>=0.28,<1.0
python-multipart>=0.0.12,<1.0

# --- Pydantic (Python 3.11 compatible) ---
pydantic>=2.5.0,<3.0
eval_type_backport>=0.2.0

# --- 文档处理 ---
python-docx>=1.1.2,<2.0

# --- 工具 ---
numpy>=1.24.0,<3.0

# --- HuggingFace 云端推理（轻量，无 torch 依赖） ---
huggingface_hub>=0.25.0
soundfile>=0.12.0

# =============================================================================
# 以下为本地音频生成依赖（体积大，仅本地开发安装）
# Render 部署不需要这些：
#   torch>=2.4.0
#   transformers>=4.46.0
#   diffusers>=0.31.0
#   accelerate>=1.0.0
#   soundfile>=0.12.0
#   safetensors>=0.4.0
# =============================================================================
