# =============================================================================
# AI 协同小说创作 Web — Python 依赖
# =============================================================================
# 安装: pip install -r requirements.txt
#
# 注意: torch 建议根据你的 CUDA 版本单独安装:
#   CPU 版:  pip install torch --index-url https://download.pytorch.org/whl/cpu
#   CUDA 12.x: pip install torch --index-url https://download.pytorch.org/whl/cu128
# =============================================================================

# --- Web 框架 ---
fastapi>=0.116,<1.0
uvicorn>=0.35,<1.0
httpx>=0.28,<1.0
python-multipart>=0.0.12,<1.0

# --- 文档处理 ---
python-docx>=1.1.2,<2.0

# --- 音频生成 (MusicGen 模型) ---
torch>=2.0.0
transformers>=4.40.0
soundfile>=0.12.0
accelerate>=1.0.0
safetensors>=0.4.0
numpy>=1.24.0,<2.0.0

# --- 可选: AudioCraft 原生集成 (需要 Python 3.10 及 xformers) ---
# audiocraft          # 从 https://github.com/facebookresearch/audiocraft 安装
# xformers<0.0.23     # AudioCraft 依赖，仅 Python 3.10 可编译
